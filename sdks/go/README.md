# narna (Go)

Go reference stub for the NARNA Universal AI Governance Runtime (UGS).

```bash
cd sdks/go
go test ./...
```

```go
import narna "github.com/hanyangkai/narna/sdks/go"

c := narna.CompileManifest(narna.Manifest{ /* … */ })
```

Specs in `/specs` remain the source of truth. Never Replace. Always Extend.
