# Ship log — 2026-07-30 (Guardian L2 runtime)

**Wedge:** Decision Layer (product) · Guardian L2 (Capability · Kill · Threat)

## Shipped

- Capability Passport **enforce-before** in adapters when `NARNA_GUARDIAN=1` or passport file present
- **Local Kill Token** — `narna kill issue|status|revoke` · `POST /v1/guardian/kill`
- **Threat Engine v0** — spawn_storm / api_flood / credential heuristics on Execution Graph
- Governor blocks killed agents; optional `NARNA_THREAT_AUTO_KILL=1`
- Tests: `tests/test_guardian_l2.py` (15 related suite green)

## Proof

```bash
narna capability evaluate --capability create.agent
narna kill issue --agent demo --reason test
narna kill status --agent demo
# threat needs a real session id after a run:
# narna threat analyze --session session_…
python -m unittest tests.test_guardian_l2 -v
```

## One-liner

> Guardian L2 live in SDK: Capability Passport gates, Local Kill Tokens, Threat heuristics on execution graphs. Decision Layer still the enterprise product — Defense in Depth, not absolute safety.

## Still missing

- True Agent Container isolation
- ~~VPS redeploy for Cloud Guardian APIs~~ → **redeployed 2026-07-30** (`api.narna.org` Decision + Guardian L2–L4 live)

---

# Ship log — 2026-07-30 (Guardian L3–L4)

**Wedge:** Collective Defense · Constitutional Governance  
**Deploy:** synced + rebuilt on `46.62.163.209` → `api.narna.org` (Decision + Guardian L2–L4 live)

## Shipped

- **L3 Collective** — opt-in threat signatures publish/import/match/apply (`src/uap/collective.py`)
- **Domain Kill** + **Global Kill** (council quorum only)
- **L4 GuardianConstitution** — levels 0–4, agents cannot amend
- **Governance Council** — propose/approve amend · domain_kill · global_kill
- Wired: constitution + collective restrict into adapter / CapabilityGovernor
- CLI: `collective` · `ai-constitution` · `council` · `kill --tier domain`
- API: `/v1/guardian/collective/*` · `/constitution/*` · `/council/*` · `/kill/domain`
- Specs: `specs/collective-defense` · `specs/guardian-constitution`
- Tests: `tests/test_guardian_l3_l4.py` (9 green with L2)

## Proof

```bash
narna collective opt-in
narna ai-constitution install
narna ai-constitution evaluate --action harm.human --strict
narna council install
narna council propose --kind global_kill --by chair --reason emergency
# second member:
# narna council approve prop_… --by security
python -m pytest tests/test_guardian_l3_l4.py -q
```

## One-liner

> Guardian L3–L4 v0: privacy-preserving threat signatures + human council / AI Constitution. Not absolute safety — detect, restrict, isolate, revoke.

---

# Ship log — 2026-07-30 (Guardian completeness)

**Wedge:** Container · Reputation · Kill cascade · Federation · Threat catalog

## Shipped

- **NGS-0016** Agent Container policy contract + CLI/API
- **NGS-0018** Agent Reputation composite → Capability mode floor
- **NGS-0019** Kill cascade (revoke → MCP disconnect → memory freeze → network isolate)
- **NGS-0017** expanded pattern catalog (exfil, scan, escalation, manip, infra…)
- **NGS-0020** federation peers push/pull/export/import-bundle
- Tests: `tests/test_guardian_complete.py` (20 Guardian suite green)

## One-liner

> Guardian Defense-in-Depth stack complete at policy-runtime v0. Host OS isolation and multi-hub federation remain infrastructure scale-up — not absolute safety.

---

# Ship log — 2026-07-31 (Wave 2 Decision Packages)

**Wedge:** Industry Decision Packages (≥6) · Tier B polish

## Shipped

- `procurement-decision` · `finance-decision` · `hr-decision` · `hospital-decision` · `crypto-decision` (+ existing `legal-decision`)
- `dmarket list --industry` · API filter
- Tests: `tests/test_decision_packages_wave2.py`
- Live: `GET /v1/dmarket/packages` on api.narna.org

## One-liner

> Decision OS marketplace now has six industry Decision Packages — legal, procurement, finance, HR, hospital, crypto.
